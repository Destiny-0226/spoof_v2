# OVO KVM AMD patch for CachyOS linux-cachyos 7.2.2-1
# Adapted from Nika amd619.mypatch (kernel 6.19) via 7.1.3-1.amd.patch.
# Same 7.x deltas as 7.1.3-1: ioapic.h context; VMMCALL -> kvm_handle_invalid_op.
# 7.2 GPR API: kvm_emulate_cpuid uses kvm_eax_read/write (no kvm_rax_write).

--- a/arch/x86/include/uapi/asm/kvm.h
+++ b/arch/x86/include/uapi/asm/kvm.h
@@ -79,7 +79,8 @@
 	__u8 elcr_mask;
 };
 
-#define KVM_IOAPIC_NUM_PINS  24
+//#define KVM_IOAPIC_NUM_PINS  24  /* NIKA removed */
+#define KVM_IOAPIC_NUM_PINS  32  /* NIKA added */
 struct kvm_ioapic_state {
 	__u64 base_address;
 	__u32 ioregsel;
--- a/arch/x86/kvm/ioapic.h
+++ b/arch/x86/kvm/ioapic.h
@@ -12,7 +12,8 @@
 struct kvm_vcpu;
 
 #define IOAPIC_NUM_PINS  KVM_IOAPIC_NUM_PINS
-#define IOAPIC_VERSION_ID 0x11	/* IOAPIC version */
+//#define IOAPIC_VERSION_ID 0x11	/* IOAPIC version */
+#define IOAPIC_VERSION_ID 0x20	/* IOAPIC version */
 #define IOAPIC_EDGE_TRIG  0
 #define IOAPIC_LEVEL_TRIG 1
 
--- a/arch/x86/kvm/lapic.c
+++ b/arch/x86/kvm/lapic.c
@@ -54,7 +54,8 @@
 #endif
 
 /* 14 is the version for Xeon and Pentium 8.4.8*/
-#define APIC_VERSION			0x14UL
+//#define APIC_VERSION			0x14UL  /* NIKA removed */
+#define APIC_VERSION			0x15UL  /* NIKA added */
 #define LAPIC_MMIO_LENGTH		(1 << 12)
 
 /*
--- a/arch/x86/kvm/svm/svm.c
+++ b/arch/x86/kvm/svm/svm.c
@@ -238,8 +238,8 @@
 
 			svm_leave_nested(vcpu);
 			/* #GP intercept is still needed for vmware backdoor */
-			if (!enable_vmware_backdoor)
-				clr_exception_intercept(svm, GP_VECTOR);
+//			if (!enable_vmware_backdoor)  // NIKA removed
+//				clr_exception_intercept(svm, GP_VECTOR);  // NIKA removed
 
 			/*
 			 * Free the nested guest state, unless we are in SMM.
@@ -837,10 +837,24 @@
 	svm_set_intercept_for_msr(vcpu, MSR_IA32_SYSENTER_ESP, MSR_TYPE_RW,
 				  guest_cpuid_is_intel_compatible(vcpu));
 
-	if (kvm_aperfmperf_in_guest(vcpu->kvm)) {
+//	if (kvm_aperfmperf_in_guest(vcpu->kvm)) {  // NIKA removed
 		svm_disable_intercept_for_msr(vcpu, MSR_IA32_APERF, MSR_TYPE_R);
 		svm_disable_intercept_for_msr(vcpu, MSR_IA32_MPERF, MSR_TYPE_R);
-	}
+		svm_set_intercept_for_msr(vcpu, 0xC00100E1, MSR_TYPE_R, true);  /* keep MSR virtualized */
+		svm_set_intercept_for_msr(vcpu, 0xC00100E0, MSR_TYPE_R, true);  /* keep MSR virtualized */
+//	}  // NIKA removed
+	svm_set_intercept_for_msr(vcpu, 0x1C9, MSR_TYPE_RW, true);  /* keep MSR virtualized */
+	svm_set_intercept_for_msr(vcpu, 0x1D9, MSR_TYPE_RW, true);  /* keep MSR virtualized */
+//	svm_enable_intercept_for_msr(vcpu,   0x1B, MSR_TYPE_R);  /* MSR_IA32_APIC_BASE */
+//	svm_enable_intercept_for_msr(vcpu,   0x3B, MSR_TYPE_R);  /* MSR_IA32_TSC_ADJUST */
+	// MSR_F15H_PERF_CTL0...5 CTR0...5
+	for (u32 msr = 0xC0010200; msr <= 0xC001020B; msr++) svm_set_intercept_for_msr(vcpu, msr, MSR_TYPE_R, true);
+	// MSR_F19H_PERF_CTL0...5 CTR0...5
+	for (u32 msr = 0xC0010290; msr <= 0xC001029B; msr++) svm_set_intercept_for_msr(vcpu, msr, MSR_TYPE_R, true);
+//	svm_disable_intercept_for_msr(vcpu, 0x179, MSR_TYPE_R);  /* MSR_IA32_MCG_CAP */
+	// MSR_AMD64_SMCA_MC0_CTL MC0_STATUS MC0_ADDR MC0_MISC ... MC31_CTL MC31_STATUS MC31_ADDR MC31_MISC
+//	for (u32 msr = 0xC0002000; msr <= 0xC00021FF; msr++) svm_disable_intercept_for_msr(vcpu, msr, MSR_TYPE_R);
+	svm_set_intercept_for_msr(vcpu, 0xC0000103, MSR_TYPE_RW, true);  /* keep MSR virtualized */
 
 	if (kvm_cpu_cap_has(X86_FEATURE_SHSTK)) {
 		bool shstk_enabled = guest_cpu_cap_has(vcpu, X86_FEATURE_SHSTK);
@@ -1141,7 +1155,8 @@
 	set_dr_intercepts(svm);
 
 	set_exception_intercept(svm, PF_VECTOR);
-	set_exception_intercept(svm, UD_VECTOR);
+//	set_exception_intercept(svm, UD_VECTOR);  // NIKA removed
+	clr_exception_intercept(svm, UD_VECTOR);  // NIKA added
 	set_exception_intercept(svm, MC_VECTOR);
 	set_exception_intercept(svm, AC_VECTOR);
 	set_exception_intercept(svm, DB_VECTOR);
@@ -1151,7 +1166,7 @@
 	 * We intercept those #GP and allow access to them anyway
 	 * as VMware does.
 	 */
-	if (enable_vmware_backdoor)
+//	if (enable_vmware_backdoor)  // NIKA removed
 		set_exception_intercept(svm, GP_VECTOR);
 
 	svm_set_intercept(svm, INTERCEPT_INTR);
@@ -1161,8 +1176,10 @@
 		svm_set_intercept(svm, INTERCEPT_SMI);
 
 	svm_set_intercept(svm, INTERCEPT_SELECTIVE_CR0);
-	svm_set_intercept(svm, INTERCEPT_RDPMC);
-	svm_set_intercept(svm, INTERCEPT_CPUID);
+//	svm_set_intercept(svm, INTERCEPT_RDPMC);  // NIKA removed
+	svm_set_intercept(svm, INTERCEPT_RDPMC);  // stability: keep KVM handling
+//	svm_set_intercept(svm, INTERCEPT_CPUID);  // NIKA removed
+	svm_set_intercept(svm, INTERCEPT_CPUID);  // NIKA: retain startup CPUID interception
 	svm_set_intercept(svm, INTERCEPT_INVD);
 	svm_set_intercept(svm, INTERCEPT_INVLPG);
 	svm_set_intercept(svm, INTERCEPT_INVLPGA);
@@ -1179,7 +1196,10 @@
 	svm_set_intercept(svm, INTERCEPT_SKINIT);
 	svm_set_intercept(svm, INTERCEPT_WBINVD);
 	svm_set_intercept(svm, INTERCEPT_XSETBV);
-	svm_set_intercept(svm, INTERCEPT_RDPRU);
+//	svm_set_intercept(svm, INTERCEPT_RDPRU);  // NIKA removed
+	svm_set_intercept(svm, INTERCEPT_RDPRU);  // stability: keep KVM handling
+	svm_clr_intercept(svm, INTERCEPT_RDTSC);  // preserve Nika timing passthrough
+	svm_clr_intercept(svm, INTERCEPT_RDTSCP);  // preserve Nika timing passthrough
 	svm_set_intercept(svm, INTERCEPT_RSM);
 
 	if (!kvm_mwait_in_guest(vcpu->kvm)) {
@@ -1860,0 +1860,13 @@
+static void svm_set_cpuid_intercept_for_each_vcpu(struct kvm_vcpu *vcpu, bool b)  // NIKA begin
+{
+	unsigned long i;
+	struct kvm_vcpu *tmp;
+	u64 tsc = rdtsc();
+	kvm_for_each_vcpu(i, tmp, vcpu->kvm) {
+		tmp->last_exit_start = tsc;
+		tmp->intercept_cpuid = b;
+		tmp->infer_intercept = true;
+		kvm_vcpu_kick(tmp);
+	}
+}  // NIKA end
+
@@ -2317,6 +2320,7 @@
 	/* Both #GP cases have zero error_code */
 	if (error_code)
 		goto reinject;
+	else if (svm_get_cpl(vcpu) == 3) { kvm_queue_exception(vcpu, UD_VECTOR); return 1; }  // NIKA added
 
 	/* Decode the instruction for usage later */
 	if (x86_decode_emulated_instruction(vcpu, 0, NULL, 0) != EMULATION_OK)
@@ -3160,10 +3179,10 @@
 		 * Suppress BTF as KVM doesn't virtualize BTF, but there's no
 		 * way to communicate lack of support to the guest.
 		 */
-		if (data & DEBUGCTLMSR_BTF) {
-			kvm_pr_unimpl_wrmsr(vcpu, MSR_IA32_DEBUGCTLMSR, data);
-			data &= ~DEBUGCTLMSR_BTF;
-		}
+//		if (data & DEBUGCTLMSR_BTF) {  // NIKA removed
+//			kvm_pr_unimpl_wrmsr(vcpu, MSR_IA32_DEBUGCTLMSR, data);  // NIKA removed
+//			data &= ~DEBUGCTLMSR_BTF;  // NIKA removed
+//		}  // NIKA removed
 
 		if (data & DEBUGCTL_RESERVED_BITS)
 			return 1;
@@ -3334,6 +3353,13 @@
 	return kvm_emulate_hypercall(vcpu);
 }
 
+static void svm_set_cpuid_intercept_for_each_vcpu(struct kvm_vcpu *vcpu, bool b);
+static int cpuid_interception(struct kvm_vcpu *vcpu)  // NIKA begin
+{
+	/* Keep CPUID intercepted permanently for guest boot stability. */
+	return kvm_emulate_cpuid(vcpu);
+}  // NIKA end
+
 static int (*const svm_exit_handlers[])(struct kvm_vcpu *vcpu) = {
 	[SVM_EXIT_READ_CR0]			= cr_interception,
 	[SVM_EXIT_READ_CR3]			= cr_interception,
@@ -3372,7 +3427,8 @@
 	[SVM_EXIT_SMI]				= smi_interception,
 	[SVM_EXIT_VINTR]			= interrupt_window_interception,
 	[SVM_EXIT_RDPMC]			= kvm_emulate_rdpmc,
-	[SVM_EXIT_CPUID]			= kvm_emulate_cpuid,
+//	[SVM_EXIT_CPUID]			= kvm_emulate_cpuid,  // NIKA removed
+	[SVM_EXIT_CPUID]			= cpuid_interception,  // NIKA added
 	[SVM_EXIT_IRET]                         = iret_interception,
 	[SVM_EXIT_INVD]                         = kvm_emulate_invd,
 	[SVM_EXIT_PAUSE]			= pause_interception,
@@ -3384,7 +3440,8 @@
 	[SVM_EXIT_TASK_SWITCH]			= task_switch_interception,
 	[SVM_EXIT_SHUTDOWN]			= shutdown_interception,
 	[SVM_EXIT_VMRUN]			= vmrun_interception,
-	[SVM_EXIT_VMMCALL]			= vmmcall_interception,
+//	[SVM_EXIT_VMMCALL]			= vmmcall_interception,  // NIKA removed
+	[SVM_EXIT_VMMCALL]			= kvm_handle_invalid_op,  // NIKA added
 	[SVM_EXIT_VMLOAD]			= vmload_interception,
 	[SVM_EXIT_VMSAVE]			= vmsave_interception,
 	[SVM_EXIT_STGI]				= stgi_interception,
@@ -4522,2 +4522,11 @@
 	sync_lapic_to_cr8(vcpu);
+
+	if (unlikely(vcpu->infer_intercept)) {  // NIKA begin
+		vcpu->infer_intercept = false;
+		if (vcpu->intercept_cpuid)
+			svm_set_intercept(svm, INTERCEPT_CPUID);
+		else
+			svm_clr_intercept(svm, INTERCEPT_CPUID);
+		vmcb_mark_dirty(svm->vmcb, VMCB_INTERCEPTS);
+	}
 
--- a/arch/x86/kvm/svm/svm.h
+++ b/arch/x86/kvm/svm/svm.h
@@ -784,7 +784,8 @@
 BUILD_SVM_MSR_BITMAP_HELPERS(void, clear, __clear)
 BUILD_SVM_MSR_BITMAP_HELPERS(void, set, __set)
 
-#define DEBUGCTL_RESERVED_BITS (~DEBUGCTLMSR_LBR)
+//#define DEBUGCTL_RESERVED_BITS (~DEBUGCTLMSR_LBR)  // NIKA removed
+#define DEBUGCTL_RESERVED_BITS (~(DEBUGCTLMSR_LBR | DEBUGCTLMSR_BTF))  /* NIKA added */
 
 /* svm.c */
 extern bool dump_invalid_vmcb;
--- a/arch/x86/kvm/x86.c
+++ b/arch/x86/kvm/x86.c
@@ -543,6 +543,9 @@
 		return 0;
 
 	if (!ignore_msrs) {
+		if (report_ignored_msrs)  // NIKA added
+			/* sudo sysctl -w kernel.printk_ratelimit_burst=1000 */
+			/* sudo dmesg [-l debug] */
 		kvm_debug_ratelimited("unhandled %s: 0x%x data 0x%llx\n",
 				      op, msr, *data);
 		return ret;
@@ -1250,7 +1253,8 @@
 	 * saving.  However, xcr0 bit 0 is always set, even if the
 	 * emulated CPU does not support XSAVE (see kvm_vcpu_reset()).
 	 */
-	valid_bits = vcpu->arch.guest_supported_xcr0 | XFEATURE_MASK_FP;
+//	valid_bits = vcpu->arch.guest_supported_xcr0 | XFEATURE_MASK_FP;  // NIKA removed
+	valid_bits = kvm_caps.supported_xcr0 | XFEATURE_MASK_FP;  // NIKA added
 	if (xcr0 & ~valid_bits)
 		return 1;
 
@@ -3557,8 +3561,8 @@
 		if (msr > last_msr)
 			return 1;
 
-		if (!(mcg_cap & MCG_CMCI_P) && (data || !msr_info->host_initiated))
-			return 1;
+//		if (!(mcg_cap & MCG_CMCI_P) && (data || !msr_info->host_initiated))  // NIKA removed
+//			return 1;  // NIKA removed
 		/* An attempt to write a 1 to a reserved bit raises #GP */
 		if (data & ~(MCI_CTL2_CMCI_EN | MCI_CTL2_CMCI_THRESHOLD_MASK))
 			return 1;
@@ -3890,6 +3894,20 @@
 		return kvm_xen_write_hypercall_page(vcpu, data);
 
 	switch (msr) {
+	case 0xC001001A:                 /* MSR_K7_TOP_MEM */
+	case 0xC001001D:                 /* MSR_K7_TOP_MEM2 */
+	case 0xC0010061:                 /* MSR_AMD64_PSTATE_CUR_LIM */
+	case 0xC0010062:                 /* MSR_AMD64_PSTATE_CTL */
+	case 0xC0010063:                 /* MSR_AMD64_PSTATE_STAT */
+	case 0xC0010064 ... 0xC001006B:  /* MSR_AMD64_PSTATE_DEF_P0...7 */
+	case 0xC00100E0 ... 0xC00100E1:  /* MSR_AMD64_MPERF APERF */
+	case 0xC00100E5:                 /* MSR_AMD64_RAPL_POWER_UNIT */
+	case 0xC00100E6:                 /* MSR_AMD64_PKG_ENERGY_STAT */
+	case 0xC00100E7:                 /* MSR_AMD64_CORE_ENERGY_STAT */
+	case 0xC00100E8:                 /* MSR_AMD64_PWR_MONITOR_CAP */
+	case 0xC0010200 ... 0xC001020B:  /* MSR_F15H_PERF_CTL0...5 CTR0...5 */
+	case 0xC0010290 ... 0xC001029B:  /* MSR_F19H_PERF_CTL0...5 CTR0...5 */
+	case 0xC0002000 ... 0xC00021FF:  /* MSR_AMD64_SMCA_MC0_CTL MC0_STATUS MC0_ADDR MC0_MISC ... MC31_CTL MC31_STATUS MC31_ADDR MC31_MISC */
 	case MSR_AMD64_NB_CFG:
 	case MSR_IA32_UCODE_WRITE:
 	case MSR_VM_HSAVE_PA:
@@ -4076,6 +4094,7 @@
 		if (!guest_cpuid_has(vcpu, X86_FEATURE_XSAVES))
 			return KVM_MSR_RET_UNSUPPORTED;
 
+		data &= vcpu->arch.guest_supported_xss;  // NIKA added
 		if (data & ~vcpu->arch.guest_supported_xss)
 			return 1;
 		if (vcpu->arch.ia32_xss == data)
@@ -4310,8 +4329,8 @@
 		if (msr > last_msr)
 			return 1;
 
-		if (!(mcg_cap & MCG_CMCI_P) && !host)
-			return 1;
+//		if (!(mcg_cap & MCG_CMCI_P) && !host)  // NIKA removed
+//			return 1;  // NIKA removed
 		offset = array_index_nospec(msr - MSR_IA32_MC0_CTL2,
 					    last_msr + 1 - MSR_IA32_MC0_CTL2);
 		data = vcpu->arch.mci_ctl2_banks[offset];
@@ -4335,6 +4354,22 @@
 int kvm_get_msr_common(struct kvm_vcpu *vcpu, struct msr_data *msr_info)
 {
 	switch (msr_info->index) {
+	case 0xC001001A:                 /* MSR_K7_TOP_MEM */
+	case 0xC001001D:                 /* MSR_K7_TOP_MEM2 */
+	case 0xC0010061:                 /* MSR_AMD64_PSTATE_CUR_LIM */
+	case 0xC0010062:                 /* MSR_AMD64_PSTATE_CTL */
+	case 0xC0010063:                 /* MSR_AMD64_PSTATE_STAT */
+	case 0xC0010064 ... 0xC001006B:  /* MSR_AMD64_PSTATE_DEF_P0...7 */
+	case 0xC00100E0 ... 0xC00100E1:  /* MSR_AMD64_MPERF APERF */
+	case 0xC00100E5:                 /* MSR_AMD64_RAPL_POWER_UNIT */
+	case 0xC00100E6:                 /* MSR_AMD64_PKG_ENERGY_STAT */
+	case 0xC00100E7:                 /* MSR_AMD64_CORE_ENERGY_STAT */
+	case 0xC00100E8:                 /* MSR_AMD64_PWR_MONITOR_CAP */
+	case 0xC0010200 ... 0xC001020B:  /* MSR_F15H_PERF_CTL0...5 CTR0...5 */
+	case 0xC0010290 ... 0xC001029B:  /* MSR_F19H_PERF_CTL0...5 CTR0...5 */
+	case 0xC0002000 ... 0xC00021FF:  /* MSR_AMD64_SMCA_MC0_CTL MC0_STATUS MC0_ADDR MC0_MISC ... MC31_CTL MC31_STATUS MC31_ADDR MC31_MISC */
+		msr_info->data = 0;
+		break;
 	case MSR_IA32_PLATFORM_ID:
 	case MSR_IA32_EBL_CR_POWERON:
 	case MSR_IA32_LASTBRANCHFROMIP:
--- a/include/linux/kvm_host.h
+++ b/include/linux/kvm_host.h
@@ -348,4 +348,8 @@
 	unsigned int halt_poll_ns;
 	bool valid_wakeup;
+
+	u64 last_exit_start;  // NIKA added
+	bool intercept_cpuid;  // NIKA added
+	bool infer_intercept;  // NIKA added
 
 #ifdef CONFIG_HAS_IOMEM
@@ -882,11 +882,15 @@
 	pr_err("kvm [%i]: " fmt, task_pid_nr(current), ## __VA_ARGS__)
 #define kvm_info(fmt, ...) \
 	pr_info("kvm [%i]: " fmt, task_pid_nr(current), ## __VA_ARGS__)
+/*#define kvm_debug(fmt, ...) \  */
+/*	pr_debug("kvm [%i]: " fmt, task_pid_nr(current), ## __VA_ARGS__)  */
 #define kvm_debug(fmt, ...) \
-	pr_debug("kvm [%i]: " fmt, task_pid_nr(current), ## __VA_ARGS__)
+	pr_err("dbg [%i]: " fmt, task_pid_nr(current), ## __VA_ARGS__)
+/*#define kvm_debug_ratelimited(fmt, ...) \  */
+/*	pr_debug_ratelimited("kvm [%i]: " fmt, task_pid_nr(current), \  */
+/*			     ## __VA_ARGS__)  */
 #define kvm_debug_ratelimited(fmt, ...) \
-	pr_debug_ratelimited("kvm [%i]: " fmt, task_pid_nr(current), \
-			     ## __VA_ARGS__)
+	pr_err("dbg_rl [%i]: " fmt, task_pid_nr(current), ## __VA_ARGS__)
 #define kvm_pr_unimpl(fmt, ...) \
 	pr_err_ratelimited("kvm [%i]: " fmt, \
 			   task_tgid_nr(current), ## __VA_ARGS__)
